import React from 'react';

const LoadingComponent = ({ website }) => (
    <div className="building-message">
        Building AI Employee for {website}
    </div>
);

export default LoadingComponent;
